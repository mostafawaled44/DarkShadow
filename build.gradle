buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.0'
    }
}

allprojects {
    // مش هنضيف repositories هنا عشان settings.gradle هي اللي هتحددها
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
